<%= form_for(@food_type) do |f| %>
  <% if @food_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food_type.errors.count, "error") %> prohibited this food_type from being saved:</h2>

      <ul>
      <% @food_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :shop_id %><br />
    <%= f.number_field :shop_id %>
  </div>
  <div class="field">
    <%= f.label :enable %><br />
    <%= f.check_box :enable %>
  </div>
  <div class="field">
    <%= f.label :sort_id %><br />
    <%= f.number_field :sort_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
