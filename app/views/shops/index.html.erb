<h1>Listing shops</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Star</th>
    <th>Logo path</th>
    <th>Enable</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @shops.each do |shop| %>
  <tr>
    <td><%= shop.name %></td>
    <td><%= shop.star %></td>
    <td><%= shop.logo_path %></td>
    <td><%= shop.enable %></td>
    <td><%= link_to 'Show', shop %></td>
    <td><%= link_to 'Edit', edit_shop_path(shop) %></td>
    <td><%= link_to 'Destroy', shop, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Shop', new_shop_path %>
